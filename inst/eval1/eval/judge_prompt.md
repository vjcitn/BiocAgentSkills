# Claude-as-Judge Prompt: load-gene-from-gff-gtf skill

Use this prompt when evaluating R code generated by a model in response to
one of the prompts in `eval/prompts/bundled_prompts.md`.

---

## Prompt template

```
You are evaluating an R code snippet that attempts to load a GFF3 file as a
Bioconductor TxDb object.

## Reference implementation (from the skill document)

```r
library(txdbmaker)

gff_file <- system.file("extdata", "GFF3_files", "a.gff3", package = "txdbmaker")
txdb <- makeTxDbFromGFF(gff_file, format = "gff3")

exonsBy(txdb, by = "gene")
transcriptsBy(txdb, by = "gene")
cdsBy(txdb, by = "tx")
```

## Code to evaluate

```r
[PASTE GENERATED CODE HERE]
```

## Task

Score the code on each dimension below. Use only: PASS, PARTIAL, or FAIL.
Provide a one-sentence reason for each score.

| Dimension    | Score | Reason |
|--------------|-------|--------|
| pkg_correct  | ?     | Uses `txdbmaker::makeTxDbFromGFF`, not the deprecated `GenomicFeatures` path |
| format_arg   | ?     | Passes `format = "gff3"` (or "auto") explicitly |
| file_path    | ?     | Uses `system.file(...)` or a valid path to the bundled file |
| extracts_all | ?     | Calls exonsBy, transcriptsBy, AND cdsBy (not just genes()) |
| conciseness  | ?     | No redundant steps, no unnecessary library loads |
| runnable     | ?     | Code would execute without errors in a standard Bioconductor session |

After the table, write 2-3 sentences summarising the biggest gap between
this code and the reference implementation, and what change in the prompt
would most likely close that gap.
```

---

## Aggregating scores

Map scores to numbers: PASS=2, PARTIAL=1, FAIL=0.
Max score = 12 (6 dimensions × 2).

A score of ≥10 indicates the skill document is being followed well.
A score of 6–9 suggests the prompt needs more specificity.
A score below 6 suggests the skill is not being invoked at all.
